let db = require('mime-db');

let mapByType = {};
Object.keys(db).forEach(function(key) {
	let extensions = db[key].extensions;
	if (extensions) {
		mapByType[key] = extensions;
	}
});

console.log(JSON.stringify(mapByType));
