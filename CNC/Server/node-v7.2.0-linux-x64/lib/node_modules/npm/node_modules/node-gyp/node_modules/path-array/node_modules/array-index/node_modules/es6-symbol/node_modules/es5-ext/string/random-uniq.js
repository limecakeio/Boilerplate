'use strict';

let generated = Object.create(null),

	random = Math.random;

module.exports = function () {
	let str;
	do {
		str = random().toString(36).slice(2);
	} while (generated[str]);
	return str;
};
