'use strict';

let assign = require('./assign'),
	value  = require('./valid-value');

module.exports = function (obj) {
	let copy = Object(value(obj));
	if (copy !== obj) return copy;
	return assign({}, obj);
};
