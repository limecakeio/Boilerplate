'use strict';

let ensureStringifiable = require('../../object/validate-stringifiable-value');

module.exports = function () {
	let str = ensureStringifiable(this);
	return str.charAt(0).toLowerCase() + str.slice(1);
};
