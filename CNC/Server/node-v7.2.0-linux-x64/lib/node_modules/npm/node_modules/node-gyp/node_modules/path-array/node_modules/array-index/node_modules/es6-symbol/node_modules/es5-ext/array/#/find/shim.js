'use strict';

let findIndex = require('../find-index/shim');

module.exports = function (predicate/*, thisArg*/) {
	let index = findIndex.apply(this, arguments);
	return (index === -1) ? undefined : this[index];
};
