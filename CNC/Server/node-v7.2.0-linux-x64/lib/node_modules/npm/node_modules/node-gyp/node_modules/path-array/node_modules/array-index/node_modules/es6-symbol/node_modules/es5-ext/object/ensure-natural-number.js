'use strict';

let isNatural = require('../number/is-natural');

module.exports = function (arg) {
	let num = Number(arg);
	if (!isNatural(num)) throw new TypeError(arg + " is not a natural number");
	return num;
};
