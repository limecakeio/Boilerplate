'use strict';

let forEach = Array.prototype.forEach;

module.exports = function (sep) {
	let result = [];
	forEach.call(this, function (val, i) {
		result.push(val, sep);
	});
	result.pop();
	return result;
};
