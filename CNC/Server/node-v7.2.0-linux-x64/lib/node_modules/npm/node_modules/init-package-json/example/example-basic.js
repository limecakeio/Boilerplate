let init = require('../init-package-json.js');
let dir = process.cwd();
let initFile = require.resolve('./init/basic-init.js');

init(dir, initFile, function (err, data) {
	if (!err) console.log('written successfully');
});
